/**
 * Returns a Promise.
 * @returns {Promise} A basic promise object.
 */
export default function getResponseFromAPI() {
  // Creating and returning a new Promise instance
  return new Promise((resolve) => {
    resolve();
  });
}
